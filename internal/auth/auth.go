package auth

// Этот файл оставлен пустым намеренно.
// Весь auth находится в:
// - handlers.go
// - jwt.go
// - middleware.go
//
// ВАЖНО: пакет auth не должен импортировать goals/tasks,
// иначе появляется import cycle (goals -> auth -> goals).
